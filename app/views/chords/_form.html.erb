<div class="inputform">
  <%= form_for(@song) do |f| %>
    <% if @song.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@song.errors.count, "error") %> prohibited this edit from being saved:</h2>

        <ul>
        <% @song.errors.full_messages.each do |msg| %>
          <li><%= msg %></li></br>
        <% end %>
        </ul>
      </div>
    <% end %>


    <div class="field">
      Chords and 
      <%= f.label "Comments" %><br />
      <%= f.text_area :comment %>
    </div>
    <div class="actions">
      <%= link_to "Cancel", :back %>
      <%= f.submit "Submit" %>
    </div>
  <% end %>
</div>
